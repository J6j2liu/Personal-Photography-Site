"use client"

import { Tab, TabGroup, TabList, TabPanel, TabPanels } from "@headlessui/react";
import Head from "next/head";
import Link from "next/link";

const tabs =  [
  {
    key: 'all',
    display: 'All'
  },
  {
    key: 'Reb',
    display: 'Rebecca'
  },
  {
    key: 'Claire', 
    display: 'Claire'
  },
]

export default function Home() {
  return (
    <div className="flex flex-col h-full bg-[url('/IMG_5060_03.jpg')] bg-top bg-contain bg-no-repeat bg-black">
      <Head>
        <title>Website</title>
        <meta name="description" content="Generated by create next app"/>
        <link rel="icon" href="/favicon.ico" />
      </Head>

    <header className="flex justify-between items-center h-[90px] px-6">
      <div className="text-transparent">hm</div>
      <div className="text-center">I need money</div>
      <Link href="#" className = "rounded-3xl bg-white text-stone-700 px-3 py-2 hover:bg-opacity-90">
        Get in touch
      </Link>

    </header>
      
      <main className="grow">
        <div className="flex flex-col h-full items-center">
          <TabGroup className="flex flex-col items-center w-full h-full">
            <TabList className="flex item-center gap-12">
              {tabs.map(tab => (
                <Tab key={tab.key} className="px-2">
                {({selected}) => (
                  <span className={selected ? "text-white" : "text-stone-600"}>
                    {tab.display}
                  </span>
                )}
                </Tab>
              ))}
            </TabList>
            <TabPanels className="bg-stone-900 bg-opacity-80 h-full max-w-[900px] w-full p-2 sm:p-4 my-6">
              <TabPanel className="">All Photos</TabPanel>
              <TabPanel>Rebecca</TabPanel>
              <TabPanel>Claire</TabPanel>
            </TabPanels>
          </TabGroup>
        </div>
      </main>

      <footer className="h-[60px] flex justify-center align-center">
        <p>Placeholder for footer</p>
      </footer>
    </div>
  );
}
